set classpath=e:\testdemo 设置解释java程序目录

set classpath=. “.”为设置默认从当前路径当中

当Java命令运行的时候实际上JVM会自动的找到CLASSPATH属性，而后找此属性对应的内容，通过指定内容设置的路径加载所需要的类。



面试题：PATH和classpath的区别
        ·PATH:是属于操作系统属性，定义所有可执行程序的路径；
        ·CLASSPATH:是JAVA程序解释类文件时所使用的加载路径。



总结：CLASSPATH主要目的是定义类的加载路径，不管定义了多少个路径，一定要定义一个“.”（便是有当前所在路径进行类的加载）
